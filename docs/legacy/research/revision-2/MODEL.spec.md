# Continuous residence model revision
User authorization: replace duplicated simple SVG art with one persistent rotating and disassembling detailed residence. Source observed canvas engine, intro2628px, toolbox3600px, modules3600px at viewport1440x900, source screenshot light #ddd9d3 linework late explosion. User supplied same reference.
Target src/components/sites/animejs-com-23cc7dc7/root-8a5edab2/residence-model.ts. Pure Three.js factory, no React, camera, lights, render loop. Export types ModelPart and ResidenceModel and createResidenceModel.
ModelPart: object:THREE.Group; home:THREE.Vector3; spread:THREE.Vector3 (delta from home, NOT absolute); spin:THREE.Vector3 (rotation delta radians); category:'shell'|'room'|'repair'|'bill'|'roof'|'base'; floor:number. Root initial position0.
ResidenceModel: root:THREE.Group; parts:ModelPart[]; surfaceMaterials:THREE.MeshStandardMaterial[]; edgeMaterials:THREE.LineBasicMaterial[]; dispose:()=>void. Keep materials original colors in material.userData.baseColor as THREE.Color and baseEmissive as THREE.Color for parent style transition. Edge geometry uses EdgesGeometry not wireframe triangles.
Original architectural model of a substantial 5-floor student dormitory, width6.8 depth4.6 height8.2 centered at origin. Base y=-4.3, five story slab tops y=-4.0,-2.5,-1.0,.5,2.0; roof y3.65. Cream precast-concrete facade, recessed smoked blue-green glass, warm yellow windows, terracotta / sage details, bronze balcony rails. Floor plates thick .15, realistic rooms arranged 3 along width, corridor at back. Balcony frontage z=2.45, modular vertical facade panels, mullions. Detailed rooftop parapet + louvered ventilation + 2 rooftop water tanks + 2 solar panel arrays. Base includes steps, paths, planters, a campus sign (can be geometry), small trees.
Split into >20 independently animatable parts: each floor slab+room furniture group (room), each floor front facade group(shell), each floor sidewall/back group(shell), roof assembly(roof), central stairs core(room), water risers and AC equipment(repair), electrical conduits and meter bank(bill), base(base). Model should expose desks, mattresses, cabinets, doors when shells peel away. Front facade separates along +z; sidewalls split along +/-x; floor plates spaced y; roof lifts/tilts; service core moves to side. Compose so full original model looks architecturally joined.
Target floor spread y=(floor-2)*1.0, x gentle stagger max1.2; facade spread z+2.3 and x according side; roof y+3.5, spin .10; repair core x+4.2; bill bank x-4.2; base y-1.5. Keep full exploded bounds <=18 high 16 wide.
Procedural geometry: merge boxes per group/material using BufferGeometryUtils mergeGeometries or equivalent. Avoid one drawcall per rail/window. Target fewer than350 draw calls and under180k triangles. Windows material emissive subtle not neon. All meshes castShadow true receiveShadow true except glass; edge outline every architectural hard edge with shared LineBasicMaterial per category and initially opacity .25. No textures/network required. Dispose all allocated geometries and materials without duplicates. No any. Use actual ThreeJS installed types. Parent handles animation and focus. Validate npx tsc --noEmit and commit only this module.
