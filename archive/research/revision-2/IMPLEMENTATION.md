# Revision 2 final implementation

Source https://animejs.com/ -> local /. Existing route updated by explicit user request; no extra routes. Original template untouched.

One persistent Three.js WebGL scene and original procedural residence replaces two SVG instances and separate service-card section. 25 independent parts: 5 interior slabs, 15 front/side facade groups, roof, stair core, water/AC assembly, electricity meters/conduits, base. Actual array count25 because floor shell count3 each plus room count1 and additional5 groups.

Timeline uses Anime.js seek against eased scroll progress. Rotation, 3D offsets, subtle per-part rotation, orthographic zoom, surface color, outline opacity and background transition continuously and reverse on scroll. Six narrative chapters use the same scene. Source modules screenshot supplied by user informed the final cream outlined exploded scene.

Three main architecture units: HomePage, ContinuousResidence, residence-model; specifications MODEL.spec.md, SEQUENCE.md and this final record. One existing font asset retained, no new downloaded visual assets. Model geometry generated locally.

Known fidelity boundary: architecture is an original five-storey dormitory instead of source mechanical engine; source section text, sponsor blocks and full toolbox not copied as user requested a campus service adaptation. Desktop and mobile use different framing. Mobile hides the final legend card to keep structure and copy unobstructed; all services remain via chapter navigation/CTA.
